# Core runtime dependencies for Galileo.
# Mirrors [project.dependencies] in pyproject.toml.
# Install with: pip install -r requirements.txt
# For test/dev tooling, also install: pip install -r requirements-dev.txt

# Core scientific stack
numpy>=1.24
astropy>=5.0

# Device protocols (optional — enable the backend(s) you need)
# (INDI needs no extra package: galileo.adapters.indi_client is a native INDI-protocol client)
# alpyca>=0.1          # ASCOM Alpaca device support
zeroconf>=0.130.0      # resolves .local mDNS hostnames (e.g. seestar.local) on Windows

# Image processing
sep>=1.2               # Source Extractor Python (star detection / HFR)
Pillow>=10.0            # FITS-cutout-to-JPEG thumbnail conversion (SKY-080, ported from Obsy)

# Image registration / photometry
astroalign>=2.4
photutils>=1.5
astroquery>=0.4.6
pandas>=1.5

# Persistence
peewee>=3.17
peewee-migrate>=1.12

# Network adapters
requests>=2.28
paramiko>=3.0          # SFTP

# Library (AstroFiler): master frames, cloud sync, smart-telescope shares, XISF import, statistics
scipy>=1.7
matplotlib>=3.3
reproject>=0.12        # WCS-based frame alignment when stacking masters
lz4>=3.0               # lz4-compressed XISF blocks
pysmb>=1.2             # SMB/CIFS shares on smart telescopes
google-cloud-storage>=2.0  # Google Cloud Storage sync (Library > Cloud)

# UI
PySide6>=6.5

# Async
qasync>=0.27
