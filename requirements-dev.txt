# Test and development tooling for Galileo.
# Mirrors [project.optional-dependencies] (test, dev) in pyproject.toml.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12

# Dev tooling
ruff
mypy
