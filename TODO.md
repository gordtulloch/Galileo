# TODO — remaining scope

What is still to be implemented, compiled from the README status, the changelog, the PSD domain list and the UI wiring as of 2026-09-23. Requirement IDs refer to [`docs/SRS.md`](docs/SRS.md). "Logic exists" means the domain module is present and tested but no screen uses it.

Out of scope for v1 (see PSD §5): post-processing/stacking beyond calibration and photometry, planetary imaging, mobile apps, a bundled guiding engine, an MCP/LLM control surface, predictive guiding.

## Tester comments
[x] ~~Framing option still on the top level menu, remove as it's been moved to the imaging screen and block~~ It wasn't actually moved yet — the top-level menu item was the *only* way to reach it, and closing that gap turned out to be the full FRAME-* redesign (`docs/PSD.md` §8.3, `docs/SRS.md` §4.8, `docs/SDD.md` §4.9), already speced by a failing test suite (`tests/test_frame.py`, 9 failing tests) and two failing `IMG-180` tests in `tests/test_img.py` from a prior session that never got implemented. Built now: `galileo/planning/framing.py` gained rotator-gated rotation (`RotatorUnavailableError`), a shared `MosaicSettings` pane-overlap singleton (FRAME-040), `attach_to_context`/`set_mosaic` (FRAME-050 — `as_sequence_target` retired from both `FramingAssistant` and `MosaicPanel`, since a mosaic now attaches as one unit, not per-panel targets), `compare_cameras`/`FovSpec` (FRAME-080), `mosaic_capture_order` (FRAME-090, pane-major round robin), and the two entry-point functions `open_from_imaging_tab`/`open_from_session_image_block` (FRAME-070) with `opening_context`/`close()`. `"framing"` removed from `PRIMARY_SECTIONS`/`_OPTICS_SECTIONS` (and so from Options too, since `OPTIONS_ITEMS` mirrors `PRIMARY_SECTIONS`); the old `_build_framing_page` primary-nav page is replaced by `AppWindow._open_framing_dialog`, a modal opened from a new **Framing…** button on the Imaging tab (`ImagingService.open_framing_assistant`/`run_mosaic_from_framing`/`active_mosaic`), built against the tab's real active optical train/camera/rotator config (`AppWindow._imaging_optical_profile`). All 14 `test_frame.py` tests and both `IMG-180` tests pass now. **Follow-up, same day:** user feedback ("the dialog should show the survey image with the FOV rectangle on it, updating live as settings change") — the initial dialog was a plain parameter form. Added: `_fetch_dss_cutout_sync` (`galileo/planning/framing.py`, the same STScI DSS/Obsy-ported technique `galileo.planning.sky_atlas`'s thumbnail fetch already uses, ADR-005, generalized to a non-square, larger preview size and cached to the user cache directory — `_cache_survey_image`/`_cached_survey_image` back the offline/cached mode, FRAME-020), `mosaic_footprint_deg` (the overall RA/Dec extent a mosaic grid covers, for sizing the fetch/preview around the whole grid rather than one pane), `survey_cutout_extent_deg` (pads the requested field so the FOV/mosaic rectangle reads as a frame against surrounding sky, not fill the whole image), and `fetch_survey_image_sync` (a synchronous, never-raising wrapper for Qt callers, falling back to any cache on failure). `AppWindow._FramingCanvas` (new, self-painted `QWidget`, same pattern as `_HistogramWidget`) shows the fetched image with the FOV rectangle — or every pane's rectangle, for a mosaic grid — overlaid; `_open_framing_dialog` now lays the parameter form out beside it, wires rotation/mosaic-grid/overlap changes straight to `canvas.set_overlay()` (no new fetch, redraws immediately), and adds a **Load Sky Image** button (deliberately not automatic per RA/Dec keystroke, to avoid a network call per edit) that auto-fires once if a target was already known when the dialog opened. New tests: `TC-FRAME-020`'s `survey_cutout_extent_is_padded_around_the_fov`/`sync_fetch_falls_back_to_cache_without_raising`, `TC-FRAME-040`'s `mosaic_footprint_matches_panel_spacing` (all in `tests/test_frame.py`) — the canvas's own paint logic isn't unit-tested, matching this codebase's existing convention for small self-painted widgets (`_AltitudeChart`, `_HistogramWidget`) having no dedicated tests of their own. **Bug found and fixed testing the above, same day:** a real (non-`minimal_profile`) wide-field optical train's dialog always showed "No sky image available" — reproduced directly: `_fetch_dss_cutout_sync`'s request, padded 3x around a several-degree FOV, exceeded what STScI's `dss_search` CGI will actually return (empirically probed live: a 120 arcmin/2.0° square cutout succeeds, 130 arcmin does not — the server presumably returns something other than FITS beyond its own limit, which `fits.open()` then can't parse, caught by the existing broad `except Exception` and silently treated as any other fetch failure). Fixed: `survey_cutout_extent_deg` now clamps its result to `_MAX_DSS_CUTOUT_ARCMIN` (120 arcmin), so a wide-field rig gets a cutout smaller than its full FOV/mosaic footprint rather than no cutout at all — verified against the live service both ways (a 50mm-lens/large-sensor profile that previously returned `b""` now returns a real image). New test `test_tc_frame_020_survey_cutout_extent_is_clamped_for_a_wide_field`. **Follow-up UX request, same day:** now that a clamped cutout can genuinely be smaller than the FOV/mosaic it's meant to frame, show that honestly rather than either clipping the rectangle border at the image edge or stretching the image to fill the rectangle. `_FramingCanvas` now fits *whichever is larger per axis* — the fetched image's extent, or the overlay's own footprint (`set_overlay`'s new `footprint_deg` arg, the mosaic footprint when one is defined, else just the FOV) — into the visible area, so the full FOV/mosaic rectangle border always stays visible at its true relative size; a dashed border around the actual image pixels marks where real survey data ends when it's smaller than the field. **Follow-up UX request, same day: rotation without a rotator.** The dialog previously just disabled Rotation entirely when `assistant.rotator_available` (profile-equipped) was false, so it did nothing observable either way. User's ask: rotation should always be usable — with a rotator actually connected, clicking OK should move it; with none connected, the tilt can't be physically achieved, so it should instead be covered by an auto-sized mosaic, with an on-screen note saying which case applies. Rebuilt around live connection state (`self._device_pages["rotator"]["adapter"]`), not the profile-equipped flag, since that's what determines whether a move is actually possible: `rotation_spin` is always enabled now, a status label reads "Rotator detected — OK will move it to this angle." or "No rotator detected — rotation will be captured as a mosaic covering the tilted field.", and two new `FramingAssistant` methods — `rotated_frame_bounding_box_deg` (the tilted single frame's axis-aligned bounding box) and `mosaic_grid_to_cover_deg` (the smallest grid covering a given area) — size an automatic covering mosaic when no rotator is connected and rotation is nonzero (an explicit mosaic grid the user set themselves always takes priority over this fallback). `_open_framing_dialog` gained `effective_grid()` (explicit choice, else the auto fallback, else `None`) driving both the live preview and the accept-time capture; on accept, a connected rotator is actually commanded via `rotator_adapter.move_to_angle()` (`asyncio.run`, matching this file's existing sync-context-async-call pattern), or the auto-mosaic is created and run via `service.run_mosaic_from_framing`. `_FramingCanvas.set_overlay` gained `reference_rotation_deg`: when the fallback is active, the auto-mosaic's (axis-aligned) panes are drawn as before, plus the originally-requested tilted single frame as a thin orange dashed outline for context — the mosaic panes themselves aren't rotated, since `create_mosaic` doesn't support rotated panel placement (unchanged, pre-existing). New tests: `TC-FRAME-030`'s `rotated_bounding_box_without_a_rotator`, `TC-FRAME-040`'s `mosaic_grid_to_cover_a_target_area` (`tests/test_frame.py`).
  **Noticed in passing, not made by this work:** the survey-image fetch (`galileo/planning/framing.py`, `galileo/planning/sky_atlas.py`) has since moved from the STScI `dss_search` CGI to the CDS `hips2fits` service — a more direct fix for the same size-limit bug the `_MAX_DSS_CUTOUT_ARCMIN` clamp above worked around, since hips2fits takes pixel size and field of view as independent parameters and doesn't fail on a wide request the way `dss_search` did. `_MAX_DSS_CUTOUT_ARCMIN` is gone, replaced by `_MAX_HIPS_FOV_DEG` (60°, a sanity ceiling rather than a tight empirical limit); `tests/test_frame.py`'s wide-field clamp test was updated to match. Recorded here since this entry's own earlier paragraphs describe the now-superseded DSS-specific mechanism.
**Still open:** constellation/coordinate-grid overlay (`FRAME-060`, `FramingAssistant.show_constellations`/`show_grid`) has no rendering in the dialog yet; `MosaicSettings`' shared overlap value is in-memory only, not yet backed by a persisted Options > Imaging field; and the Session Image block's own Framing… control (`SES-130`) is still the pre-existing no-op hook — wiring it needs block-parameter-editing UI that doesn't exist at all yet (`ImageBlock`'s exposure/count/filter/binning/gain/offset fields have no UI either), which is its own, larger piece of work.
[x] ~~When a mosaic is set up in the Framing function, nothing shows that Capture will shoot a mosaic instead of a single frame — and no code actually executed a defined mosaic pane by pane.~~ Both parts were real gaps, confirmed against source before fixing: `ImagingService.run_mosaic_from_framing` only ever set `active_mosaic`, which nothing read back — Capture always took a plain single-position series regardless. Fixed: `mosaic_capture_order` (FRAME-090's pane-major walk) is now a module-level `galileo.planning.framing` function (was a `FramingAssistant` method needing optical-train state it never used) so it can be called without building an assistant; new `ImagingService.capture_mosaic()` walks it, re-slewing the mount (`ImagingService._mount`, refreshed by the page like `_camera` already is) to each pane's centre — precessed J2000→JNow the same way `AppWindow._mount_to_object` does — waiting for the slew (`galileo.tracking.wait_for_slew`) then exposing via the existing `capture_and_preview`/auto-save path; the re-slew between passes is the dither, so no guider-dither command is sent. `_MosaicCaptureThread` (mirrors `_CaptureThread`) runs it off the UI thread; `do_capture()` branches into it whenever a mosaic is active, refusing with a message (not a silent single frame) when no mount is connected. **Visual indication:** the Imaging page now shows a status line ("Mosaic active — C×R panels (N total)…") and Capture itself relabels to "Capture Mosaic" whenever a mosaic is active; a new Clear Mosaic button reverts to a single frame, and reopening Framing and dialing the grid back to 1×1 clears it too (previously a stale mosaic from an earlier visit stayed silently active). New tests: `tests/test_img.py`'s `TC-IMG-180` mosaic-execution suite (pane-major slew/expose order, the mosaic-without-a-mount/without-a-mosaic refusals, Stop ending it after the current pane, and the button/label UI toggle), `docs/SDD.md` §4.9 updated.
[x] ~~"Add a 'Add to Session' option to the right click context menu in the star atlas. Currently when the user clicks on a star the target is created as a session which is incorrect."~~ `AppWindow._set_current_object` (driven by every click — left, double, or the right-click that opens the context menu) used to also auto-create a session as a side effect, so a plain click looked identical to deliberately building one. Removed that side effect; session creation is now exclusively the explicit **Add to Session** action, added to the Star Atlas's right-click menu (alongside Goto/Sync) and going through a new shared `AppWindow._add_to_session(name, ra_deg, dec_deg)` (`SES-160`) — the same entry point the Targets page's own Add to Session tile button was refactored to call too, so the two can't drift apart. New tests in `tests/test_star_atlas.py`: a plain click no longer calls `create_session_for_target`; the context menu offers Goto/Sync/Add to Session and triggering the last one creates the session with the right target. `docs/SDD.md` §4.8/§4.9a updated.
[x] ~~Planning / Targets screen still very basic.~~ Rebuilt across several passes this session (full narrative in `CHANGELOG.md`'s Added/Fixed entries, this is the summary): **Search** — catalog-first/Simbad-fallback (`SKY-100`, was backwards), object type, size range (min+max), magnitude, Visible tonight (with configurable altitude threshold + minimum duration), Min. Moon separation, and a Messier/Caldwell/NGC Catalog filter (reusing `catalogs_of()`/`DSO_CATALOGS`, pre-existing code the Star Atlas/skymap already used but never wired into this screen). Optional Telescopius search augmentation/observing-list import (`SKY-110`/`SKY-120`) is gated/plumbed but the actual REST calls are deliberately stubbed (`NotImplementedError`) since Telescopius's own public API isn't documented/stable enough to build against yet (`docs/PSD.md` §6.9). **Results** — one white-bordered tile per row (not a grid — tried, explicitly reverted per feedback), each narrowed to 50% of the list's width (`_TILE_WIDTH_FRACTION`, `_tile_row`), carrying everything a since-removed details panel used to show on selection: name/type/magnitude/size/constellation/RA/Dec, rise/transit/set, a mini altitude-over-the-night chart with a labelled time axis (the shared `_AltitudeChart` widget, also used by the Scheduler's Show Trajectory dialog — its own chart gained the same time labels for free), and — rightmost — three per-tile action buttons: **Select** (makes it the Pier's current object, `IMG-140`), **Slew To** (immediately commands the connected mount there, reusing the same `_mount_to_object` the skymap's slew-to-clicked-location already uses), and **Add to Session** (creates a new session pre-populated with a Target block, `SES-160`, reusing the same hook `_set_current_object` already calls elsewhere) — all three reuse existing cross-module entry points rather than adding new ones, and all three degrade to a status-bar message rather than crashing when their precondition isn't met. Chart/rise-set data for every tile is computed in one batched astropy transform (`SkyAtlas.altitude_charts_batch`, ~1s for 200 results vs. ~10s individually); thumbnails stay individually fetched, capped at the first 12, and disk-cached by ra/dec/field-size so a repeat fetch is a disk read. **Follow-up, same day:** clicking a tile's thumbnail now opens a full-size (640px) view of the same field in a modal dialog, fetched and cached separately from the small 150px tile crop (`SkyAtlas._fetch_thumbnail`'s new `size_px` parameter) rather than upscaling it; **Select** now also adds the object to `SkyAtlas`'s own target list (`add_to_target_list`, via a new persistent `AppWindow._shared_sky_atlas()` — see the resolved "Known gaps" entry below for why a persistent instance mattered here specifically). Still open: the Telescopius-reference visual/export polish (colour scheme, print/CSV/target-list export) and Options UI to enter a Telescopius API key.

 
## Placeholder screens ("not implemented yet")

- [x] ~~**Planning > Sequence** — now the **Sessions** screen (`SES`, formerly `SEQ`/`SEQ-ADV`; SRS §4.5 execution engine, §4.5a screen authoring/lifecycle, §4.6 nested instruction/condition/trigger blocks). There is no `galileo.ui.sessions` module at all yet — `tests/test_ses.py`'s `SES-100`–`230` range is written against a screen that doesn't exist.~~ Built: `galileo/ui/sessions.py` — a pure-Python model (`SessionsScreen`, `SessionRegion`, 17 block classes SES-130/140, `SessionTemplate`, `BlockOrderError`/`RegionLockedError`) with a thin Qt view (`SessionsPageWidget`: right-hand drag-and-drop action palette via `QListWidget`, one bordered/scrollable card per session region with Save/Save as Template/Load from Template/Schedule↔Deschedule/Delete). Wired in at Planning > Sessions (`PLANNING_ITEMS` relabeled from "Sequence"), registered in `self._device_pages["sessions"]` per the standing per-Pier reload convention. Selecting a target on the Star Atlas now auto-creates a session with a Target block (SES-160, verified end-to-end against a real `AppWindow`). `.gstpl` templates and per-Pier `.gses` session files use JSON, not the Peewee table SDD §4.6a specifies (`SessionTemplate` "in the same database, ADR-002") — **this was wrongly logged here as a deliberate, already-reconciled deviation; it isn't.** `CLAUDE.md`'s persistence-unification policy and SDD's own Data Design table (§5) both call for the shared Peewee database, and this JSON approach is a real gap against that, not a validated alternative — see the correction two lines below and in `docs/SDD.md` §4.6a, now annotated with "Current implementation" gap notes rather than silently rewritten to match the shortcut. Using plain `QListWidget` drag/drop instead of a `QGraphicsView` canvas remains a considered, still-valid choice (no drag-and-drop precedent existed anywhere in this codebase to extend, and nothing in the tests needs a graphics-scene canvas) — that part of the original claim stands. Block execution against real hardware is explicitly out of scope for this screen — see the "Domain logic with no UI" items below for Autofocus/Solve/Calibration/MeridianFlip/Safety/Dome/Notifications, which is where that wiring belongs. Prerequisite fixes: `EquipmentProfile.from_dict` classmethod added (the `sessions_screen` test fixture referenced a nonexistent `Profile` name); `WaitUntilTimeCondition`/`WaitForAltitudeCondition` added to `galileo.sequencer.advanced`, fixing the previously-failing `TC-SES-310`. All 14 `SES-100`–`230` tests plus `TC-SES-310` pass; full suite otherwise unchanged.
- [x] ~~**Planning > Scheduler** (`SCHED`) — job queue UI, constraints, multi-night progress. Logic exists (`galileo.scheduler`).~~ Built: `galileo/ui/scheduler.py` (`SchedulerPageWidget`) — a table of the active Pier's job queue (priority, target, state, progress, start/completion condition) with Move Up/Down, Edit… (priority, constraints, startup/completion conditions, frames required — a real dialog, `_EditJobDialog`), Remove, and Show Trajectory… (a small self-painted altitude-over-time chart, SCHED-080, reusing the already-tested `ObservatoryScheduler.get_job_trajectory_chart`). Wired in at Planning > Scheduler, registered in `self._device_pages["scheduler"]`. The screen never creates jobs directly — only Planning > Sessions' Schedule control does (`SES-200`) — so scheduler-instance ownership moved from `SessionsPageWidget` up to `AppWindow._scheduler_for_pier` (one `ObservatoryScheduler` per Pier, shared by both screens; verified end-to-end that scheduling from Sessions is immediately visible in Scheduler). **Scope addition** (user's call, mid-build): a job that completes successfully (not one manually removed/desecheduled) now deletes its originating session outright rather than returning it to editable draft — new `SchedulerJob.is_complete`/`ObservatoryScheduler.reap_completed_jobs()`, called from both screens' `reload()`. Backported into `docs/SRS.md` (`SES-210`, `SCHED-050` text extended — no new requirement IDs, same SDD-component mapping), `docs/SDD.md` §4.6a/§4.9b, and `docs/PSD.md` §8.1's Schedule/Deschedule narrative. **Correction to this entry's own earlier claim:** the first draft of this backport wrongly "corrected" §4.9b to say JSON persistence is the intended design, when `CLAUDE.md` and SDD's own Data Design table (§5) both explicitly call for the scheduler job queue to live in the shared Peewee database — caught when asked directly why persistence was JSON rather than Peewee. Fixed properly: §4.6a/§4.9b now describe the Peewee-based target architecture (unchanged) with explicit "Current implementation" gap notes for what's actually built (JSON files for both `SessionTemplate` and the job queue; no `SequenceRunner`/`SequencerNode` integration yet either — session authoring and scheduling are self-contained, not yet wired to the execution engine). New "Known gap" below: migrate `SessionTemplate` and `ObservatoryScheduler`'s persistence from JSON to the shared Peewee database, likely as one migration since both gaps have the same shape and were found together. New tests: `test_tc_sched_050_completed_job_is_reaped_and_its_session_deleted`/`..._not_reaped` and three UI-level tests in `tests/test_sched.py`, plus `test_tc_ses_210_completed_job_deletes_the_session_not_deschedules_it` in `tests/test_ses.py` (RTM already traced `SES-210` to both `galileo.ui.sessions` and `galileo.scheduler`).
- [ ] **Options** — the menu and a page per section exist. **Library** (AstroFiler's configuration screen), **Star Atlas** (horizon file upload), **Planning** (the "do not slew where obstructed" option, and now also a "Cache All Catalog Thumbnails…" button that bulk pre-fetches every catalog object's survey-image thumbnail off the UI thread via a cancellable progress dialog — `AppWindow._ThumbnailCacheThread`, reusing `SkyAtlas._fetch_thumbnail`'s own disk cache so an already-cached object is skipped for free) and **Imaging** (`IMG-170`'s saved-frame BITPIX/sample-format picker) have real settings; the other pages (Equipment, Guiding, Focus, Solve, Science) are still placeholders, and theme/layout customization (`UI`), notification configuration and per-user preferences are not built. (Options > Framing no longer exists — Framing isn't a primary section any more, see the Tester comments entry above.)

## Domain logic with no UI

- [ ] Plate solving (`PLT`) — the Solve screen (`galileo/ui/solve.py`) captures, solves, syncs/slews and shows results. Still to do: solver configuration UI (ASTAP path, search radius, field-of-view hint per profile — `PLT-060`), the astrometry.net backend (`_run_astrometry` is a stub), Mount Model and Polar Alignment, Bin/Gain/ISO/Dark/Filter options for the solve exposure (the camera capture path can't set them), and an Options > Solve page. `PlateSolver.solve_and_center` (`PLT-040`; nothing calls it yet — the sequencer and meridian flip, `MFLIP-030`, are meant to) slews back to the target without first syncing to the solution, so it cannot correct a pointing error, and compares RA without `cos(Dec)`; when those callers are written, use `SolveWorkflow` (sync-then-slew) or fix it first.
- [ ] Calibration / Flat Wizard (`CAL`).
- [ ] Meridian flip (`MFLIP`).
- [ ] Safety and weather (`SAFE`) — unsafe-condition handling, safe-park.
- [ ] Dome slaving (`DOME`).
- [ ] Session history review (`HIST`).
- [ ] Notifications configuration (`NOTIF`).
- [ ] Multi-Pier status dashboard (`OBS`).

## Known gaps

- [x] ~~Equipment > Camera page had no Connect button at all (any protocol, not just INDI) — `EQP-010`'s "connect, disconnect, and live connection-status" requirement was silently unmet for the Camera category alone.~~ Reported as: creating/configuring an INDI camera left the Imaging tab showing it as "not connected," with no way to connect it from the Camera page. Root cause: `_build_camera_panel`'s header only ever had Remove (no Connect, unlike every other category's own panel — Mount/Focuser/Rotator/Filter Wheel all have one); the only thing that ever populated a live camera backend was `autoconnect_page()`, called at page build and Pier switch only, never after Save — so a freshly configured camera stayed unreachable until an app restart or Pier switch happened to run it. Fixed: `_build_camera_panel` gained a `Connect` button per panel (mirroring Focuser's, wired to the existing `_connect_camera_device`/`_camera_backends` machinery); `save_camera_config` now also calls `autoconnect_page()` after saving, so Save alone connects a newly configured device without a manual Connect click; `autoconnect_page()` gained a guard to skip already-connected slots, making it safe to call from more than one place without disrupting an unrelated (or the same) panel's live connection. New tests `test_tc_eqp_010_camera_page_has_a_connect_button_per_panel`/`..._camera_connect_button_connects_the_device`/`..._saving_camera_config_auto_connects_a_newly_configured_device` (`tests/test_eqp.py`) — the last of these also confirms the Imaging tab's camera selector reflects the connected state without a Pier switch, the second half of the reported symptom.
- [x] ~~Library: `fitsProcessing.createMasterCalibrationFrames` (the older `galileo.library.core.calibration` path) creates no masters — it looks sessions up by an id that is `None`.~~ Removed: `galileo/library/core/calibration.py` (`CalibrationProcessor`) and its three `fitsProcessing` wrapper methods (`createMasterCalibrationFrames`, `createMasterCalibrationFramesForSessions`, `checkCalibrationSessionsForMasters`) had zero callers anywhere in the app, CLI or tests — the working path is `galileo.library.core.auto_calibration`, already used everywhere. Deleted rather than repaired a broken, unreachable duplicate.
- [x] ~~Library: the Mappings screen's **Reorganize Files** and **Apply Mappings** actions are no-ops.~~ The six dead-end methods this described (`apply_database_mappings`, `reorganize_repository_files`, `reorganize_file`, `calculate_new_file_path`, `cleanup_empty_directories`, `apply_file_folder_mappings`) were never wired to any button and duplicated the real, working per-row Apply path — removed. That real path (`apply_single_mapping`) now honours all three checkboxes (Apply mappings to database / Update FITS headers on disk / Reorganize repository folders) independently instead of ignoring two of them; new test `test_mappings_apply_button_gates_each_effect_on_its_own_checkbox`.
- [x] ~~Library: `auto_calibration.py`'s quality-assessment step accepts a `generate_report` flag but never writes one.~~ `generate_quality_report()` now writes a per-file CSV (star count, FWHM, eccentricity, HFR, SNR, image scale, a summary row) to a new `galileo.platform.get_reports_dir()`; wired into `perform_quality_assessment`'s `generate_report` path, which the `galileo-auto-calibration --report` CLI flag and Options-driven quality runs both reach. New test `test_tc_lib_070_quality_assessment_writes_a_report_when_requested`.
- [x] ~~Library: `auto_calibration.py`'s workflow orchestrator reports a hardcoded `light_frames_calibrated = 10`.~~ `runAutoCalibrationWorkflow`'s `calibrate` step now diffs `get_calibration_statistics()`'s `calibrated_frames` before/after the run for the real count — the same number the Auto-Calibration dialog displays to the user. New test `test_tc_lib_060_workflow_reports_the_real_calibrated_frame_count`.
- [x] ~~Library: the repository statistics dashboard (`LIB-080`, `TC-LIB-080`) was removed from the Library on request and is not built; either reinstate it or retire the requirement in the SRS/RTM.~~ Retired (user's call): `LIB-080` is gone from `docs/SRS.md` §4.22 (left as a documented gap, not renumbered — same convention as `EXT-100`/`SEQ-ADV`), its `docs/RTM.md` row is removed, and the now-permanently-skipping `test_tc_lib_080_repository_statistics_dashboard` (it `importorskip`'d a `galileo.library.core.statistics` module that never existed) is deleted. Domain/total counts updated in both documents; they net back to their pre-session values (SRS 258, RTM 272) since this session's other change added one requirement (`NFR-SEC-030`) while this one removed one.
- [ ] Library: two source-adapter sets coexist — `galileo.library.adapters` (SMB/FTP/SFTP, small) and `galileo.library.services.telescope` (AstroFiler's full smart-telescope manager, used by the Download dialog). Merge them behind the `RemoteSourceAdapter` interface in SDD §4.23.
- [x] ~~Library: the iTelescope password is stored in plain text in `library.ini` (as AstroFiler did); move it to the OS keychain (`NFR-SEC`).~~ New `NFR-SEC-030` requirement (`docs/SRS.md`/`SDD.md`/`RTM.md` updated, `TC-NFR-SEC-030`), since none of the existing `NFR-SEC` requirements actually covered credentials-at-rest. `galileo.library.config.get_itelescope_password`/`set_itelescope_password` wrap the `keyring` package (already a transitive dependency via `astroquery`, now declared directly), with try/except around every call so a missing/locked backend degrades to "no password" rather than crashing. A plaintext `itelescope_password` left by an older version is migrated into the keychain and stripped from `library.ini` the first time it's read. Wired into `config_widget.py` (Options > Library save/load), `telescope.py`'s `get_itelescope_credentials`, and `download.py`'s CLI pre-flight check. New test `test_tc_nfr_sec_030_itelescope_password_stored_in_keychain_not_plaintext` (`tests/test_nfr.py`), with the OS keychain faked via `monkeypatch` so it never touches the real one.
- [ ] Library: the Sessions and Images screens run their queries on the UI thread; move the slow ones to a worker (`ARCH`, concurrency model).
- [x] ~~**AWAITING USER DECISION:** `SES`/`SCHED` persistence uses JSON files (`.gstpl` session templates, per-Pier `.gses`/scheduler-progress files), not the shared Peewee database `CLAUDE.md` and SDD §5's Data Design table both specify for these (`SessionTemplate`, "Scheduler job queue and per-job multi-night progress").~~ User's call: migrate now. New migration `016_add_session_and_scheduler_persistence.py` adds `session_templates` and `scheduler_jobs` tables (`galileo.library.models.session_template.SessionTemplateRecord`, `galileo.library.models.scheduler.SchedulerJobRecord`). `SessionRegion.save_as_template`/`SessionTemplate.load`/`list_names` now take a name, not a path (Save/Load as Template's file dialogs became a name prompt and a picker over existing names, `galileo/ui/sessions.py`); `SessionRegion.save()`'s own `.gses` per-session file is unchanged (that one already matched its target design — SES-060 maps to SDD's "Sequence definition" Data Design row, which specifies a user-chosen JSON file, not Peewee). `ObservatoryScheduler.set_persistence(path)` became `set_persistence(pier_name)`, scoping an instance to its Pier's rows in the shared `scheduler_jobs` table rather than pointing at a separate file; every queue mutator (`add_job`/`remove_job`/`move_job`/`update_job`/`reap_completed_jobs`/`record_frames_captured`) now calls `save()`, which is a no-op until `set_persistence` is called (matches every pre-existing test that never enabled persistence). `AppWindow._scheduler_for_pier` now calls `set_persistence`+`load()` when it first creates a Pier's scheduler — previously the running app never called `set_persistence` at all, so SCHED-100 wasn't actually met even with working JSON code; this closes that too, not just the storage format. Constraints/startup/completion conditions are now persisted as JSON blob columns on each job row — the old JSON `save()` silently dropped them, and the separate `set_progress_store()`/`_flush_progress()` path (a second file `load()` never read back) is retired into the one `save()`/`load()` pair. `docs/SDD.md` §4.6a/§4.9b updated to describe the implemented state rather than a gap. Tests: `tests/test_ses.py`'s `TC-SES-180`/`TC-SES-190` rewritten against named DB templates (`init_db` per test); `tests/test_sched.py`'s `TC-SCHED-100` likewise; `TC-SCHED-090` simplified (no longer needs a progress-store path). Full suite green after the change (see below).
- [ ] `SES`/`SCHED` have no execution-engine integration: `galileo.ui.sessions`' blocks don't translate into `galileo.sequencer.basic`/`.advanced`'s `SequenceRunner`/`SequencerNode`, and `galileo.scheduler.SchedulerJob` doesn't trigger one becoming runnable — `SchedulerJob.sequence` holds a `SessionRegion` object, not a `SequenceDef`. This was always the explicit scope boundary for the Sessions/Scheduler screen builds (see the two entries above), not a new finding, but is called out here as its own line since it's the same shape of gap as the persistence one above and belongs in the same future pass, not scattered across two placeholder-screen entries above.
- [ ] Star Atlas: comets/asteroids/satellites (`SKYMAP-040`), FOV/mount overlay (`SKYMAP-050`), slew-from-map polish (`SKYMAP-060`).
- [ ] Equipment pages poll devices on the UI thread while visible; move to a worker thread.
- [x] ~~Parked-mount guard does not cover Sync or tracking on/off.~~ Both `IndiMountAdapter`/`AlpacaMountAdapter`'s `sync_to_coordinates` and `set_tracking(True)` now go through `_refuse_if_parked` first, same as slew/jog/find-home — syncing while parked would calibrate the pointing model against a bogus position, and enabling tracking while mechanically parked is nonsensical/risky on some mounts. `set_tracking(False)` (and a jog's `rate=0` stop) stay unconditional, matching the existing "stopping is always allowed" pattern. Extended `TC-EQP-MNT-010`'s existing INDI and Alpaca tests rather than adding new ones, since its own docstring already claimed this coverage.
- [ ] Horizon obstruction guard (`SKYMAP-080`, `galileo.core.slew_guard`) checks only the slew destination, not the path the mount takes; it skips jogging and Find Home, treats RA/Dec as of date, and does nothing for an Observatory with no latitude/longitude.
- [ ] Plugin framework (`PLUG`): marketplace raises `NotImplementedError` (`galileo/plugins/__init__.py`); `VST`/`VST-AN` moved to `galileo/plugins/vstarget/` (nested under the plugin framework, per its updated ADR-VST-001) but still pre-loaded/imported directly rather than discovered via entry points — not yet a genuinely installable first-party plugin.
- [x] ~~`galileo/vstarget/analysis/transform_apply.py` applies a placeholder correction (`Tv * 0.05`); implement the real transformation formula.~~ `apply_transformation` (now `galileo/plugins/vstarget/analysis/transform_apply.py`) takes the whole set of same-epoch, same-target measurements across filters (matching VST-AN-070's literal "multi-filter observations" wording, which a single-``PhotometryResult`` signature couldn't satisfy — a colour index needs two filters) and applies the real AAVSO colour transformation: B/V pair -> `(B-V)_std = Tbv*(b-v)`, `V' = v + Tv*(B-V)_std`, `B' = V' + (B-V)_std`; V/R pair used as a fallback via `Tvr`/`Tr` when there's no B. A lone filter with no companion is passed through unchanged with `is_transformed=False` rather than claiming a correction that was never computed. Test `test_tc_vst_an_070_apply_transformation_to_multifilter` rewritten to assert the actual corrected magnitudes, not just that *something* changed.
- [x] ~~Framing (`FRAME`): verify survey-image overlay, mosaic-to-sequencer handoff and constellation/grid overlay against the SRS; the README lists only the input panel as working.~~ Superseded by the Tester comments entry above, which covers this in full — the "mosaic-to-sequencer handoff" model itself was retired (a mosaic attaches as one unit, `FRAME-050`), and survey-image/constellation-grid overlay remains an open item, tracked there instead of duplicated here.
- [x] ~~Planning (`SKY`): horizon-profile editing (`SKY-040`; upload, display and clearing exist under Options > Star Atlas, but there is no point-by-point editing).~~ Options > Star Atlas's horizon table now has **Add Point**, **Remove Selected** and **Save Changes** alongside Upload/Clear — the table is directly editable (double-click a cell), Add Point appends a `(0, 0)` row ready to type over, Save Changes validates every row (0–360° azimuth, 0–90° altitude, both numeric) before writing, rejecting the whole save with the offending row number rather than partially saving. New test `test_options_star_atlas_edits_horizon_points_directly` (`tests/test_star_atlas.py`), tagged `TC-SKY-040` alongside the existing domain-level test for that same requirement.
- [x] ~~Planning (`SKY-080`): `SkyAtlas.add_to_target_list`/`_fetch_thumbnail` (the DSS thumbnail-caching logic) is real but genuinely unreachable — the live "pick a target" flow goes through a separate, parallel mechanism (`galileo.current_object.CurrentObject`/`get_current_objects()`, `IMG-140`) that `_set_current_object` (`galileo/ui/app_window.py`) writes to instead; `SkyAtlas._target_list` is never touched by the UI at all. Needs a design decision — call the thumbnail-caching logic from `_set_current_object` (folding SKY-080 into the `current_object` flow), or reconcile/retire the separate `_target_list` concept — not just adding a missing call.~~ Decided (user's call): wire it into Select rather than retire it. Note this gap is narrower than when first written — this session's other SKY-080 work (per-tile thumbnails, the bulk-cache button) already made `_fetch_thumbnail` itself reachable via search results; only `add_to_target_list`/`_target_list` specifically remained orphaned. New `AppWindow._select_result` (the Targets page's **Select** button, replacing its direct `_set_current_object` call) does both: sets the current object as before, and calls `add_to_target_list` on a new `AppWindow._shared_sky_atlas()` — a single persistent `SkyAtlas` instance, not a fresh one per call, since `_target_list`'s whole point is accumulating across selections, which the page's existing throwaway-per-call instances would have silently defeated. A thumbnail-caching failure inside `add_to_target_list` never blocks Select itself. New test `test_tc_sky_080_select_result_adds_it_to_the_target_list`; verified live that two Select clicks accumulate on the same shared instance (`["M42", "M31"]`, not reset between clicks). `docs/SDD.md` §4.8 updated.
- [ ] Planning (`SKY-090`): geocoding — `galileo.planning.sky_atlas.geocode_location` exists and is tested at the domain level (`TC-SKY-090`, `test_tc_sky_090_geocode_location_name`), but nothing in the UI calls it. Was briefly wired into the New Observatory dialog (a "Look up" button next to Physical Address, `_prompt_new_observatory`, plus `AppWindow._geocode_observatory_address`) in an earlier pass this session, then explicitly removed on request as not required — the dialog-level test (`test_tc_sky_090_new_observatory_dialog_look_up_button_fills_coordinates`) was deleted along with it. The domain function/test are untouched and still satisfy `SKY-090`'s own requirement text (which doesn't mandate a specific UI entry point); this line just tracks that no UI currently calls it, a deliberate state rather than an oversight.

## Non-functional and release

- [ ] `NFR-INSTALL` — one-step installer per platform (current launch scripts are temporary).
- [ ] `NFR-PORT` — verify on macOS, Linux and Raspberry Pi 5; only Windows appears exercised.
- [ ] `NFR-I18N` — confirm all UI strings are externalized.
- [ ] `NFR-SEC` — review handling of WAN-exposed INDI/Alpaca endpoints.
- [ ] `NFR-REL` — soak tests and an overnight run with transient disconnects.
- [ ] Hardware/integration tests against the reference observatory (roll-off roof, two Seestars) and real PHD2/solver installs.
- [ ] CI — no `.github/` workflow; add lint (`ruff`), types (`mypy`) and test runs.
- [ ] Crash reporting (`LOG`) — confirm implemented.
- [ ] `alpyca` is commented out in `pyproject.toml`; confirm the Alpaca adapters don't need it.
- [ ] Keep the README status section and `CHANGELOG.md` current as items close.
